# تحديث ITKF — تعديل الطلب بنفس رقمه

الإصدار الحالي: `2026-08-31-v4`. هذه ملفات تحديث للمشروع الحالي؛ لا تنشئي مشروعًا أو شيتًا جديدًا.

## 1. التثبيت — مهم: التطبيق وGoogle معًا

1. خدي نسخة احتياطية من ملفات المشروع والشيت أولًا. حدّثي في وقت هادئ؛ إعادة تشغيل Streamlit تنهي جلسات الإدخال المفتوحة.
2. في نفس مشروع GitHub استبدلي الملفات الستة معًا، بنفس الأسماء وفي Commit واحد:
   - `app.py`
   - `config.py`
   - `helpers.py`
   - `sheets.py`
   - `pdf_generator.py`
   - `requirements.txt`
3. احتفظي بملف `countries.py`، ومجلد `assets` واللوجوهات والخطوط، وإعدادات Light Mode والأسرار. الحزمة لا تحتوي لوجوهات بديلة؛ لا تمسحي اللوجوهات الموجودة عندك.
4. افتحي **نفس مشروع Google Apps Script**. استبدلي محتوى `Code.gs` بمحتوى `google_apps_script.gs` المرفق. ملف Google لا يُشغَّل بواسطة Streamlit؛ رفعه في GitHub فقط لا يغيّر الخدمة المنشورة.
5. اعملي Save. من قائمة الدوال بجوار Run اختاري `setupSheetsNow` ثم Run. هذا يضيف أعمدة التعديل وورقة `Request History` دون مسح الطلبات أو إعادة ضبط حصص الغرف المعدّلة.
6. اختاري **Deploy → Manage deployments → علامة القلم → Version: New version → Deploy**. احتفظي بنفس رابط `/exec` ونفس الصلاحيات والأسرار.
7. شغّلي `installRetryTrigger` مرة واحدة إن لم يكن مفعّلًا. لا تنشئي محفزات متكررة يدويًا. يشغّل إعادة محاولة إرسال الفواتير الموجودة في Drive كل 5 دقائق، حسب حصة Google.
8. اعملي **Reboot app** في Streamlit، ثم نفّذي اختبار القسم 7 أدناه قبل فتح التعديل للعملاء.

التطبيق يتحقق من توافق الإصدار. إذا ظهر تنبيه اختلاف النسخ، تأكدي من رفع الملفات الستة ونشر New version في Google، وليس Save فقط.

## 2. ما الذي تغيّر؟

- اسم زر الخطوة `Personal` أصبح **Details**، وعنوان الصفحة **Registration Details**؛ مناسب للاتحاد والفرد.
- زر واضح في شاشة البداية: **View / edit existing request**.
- كل طلب له **Request ID ثابت**. الرقم يظهر على شاشة النجاح وPDF والإيميل.
- للتعديل: رقم الطلب + الإيميل المسجّل + كود تحقق يُرسل لهذا الإيميل. الرقم وحده لا يفتح أي بيانات.
- فتح الطلب يعرض البيانات المحفوظة ورقم الإصدار وPDF وحالة الإيميل. **Edit this request** يعيد بياناته إلى نفس خطوات التسجيل، مع الحفاظ على حجز الأيام المتكررة.
- **Save Changes & Send Updated PDF** يحدّث نفس صف `Bookings`، ولا ينشئ طلبًا آخر.
- عند التعديل يعاد فحص التواريخ والحقول والجواز وسعة العربيات والحصص تحت قفل Google. يتم استبعاد حصة نفس الطلب من حساب الغرف المستخدمة؛ لا يُحجز مرتين.
- لو الغرف الجديدة غير متاحة، يبقى الطلب السابق كما هو. لو عُدّل الطلب من جلسة أخرى، يُرفض حفظ النسخة القديمة ويُطلب فتح أحدث نسخة.
- إعادة محاولة حفظ نفس التعديل بعد انقطاع الرد لا تنشئ إصدارًا آخر ولا تضاعف الغرف.
- ما زال الانتقال من أزرار الخطوات حرًا للمعاينة، وNext والإرسال يراجعان البيانات المطلوبة. تليفون الاتحاد اختياري، وتليفون الفرد مطلوب.
- الأسماء/الفنادق/الأسعار/الوجبات/سعات العربيات/حصص الغرف السابقة لم تتغيّر في هذا التحديث.

## 3. حماية التعديل

- كود التحقق 8 أرقام، صالح 10 دقائق، بحد أقصى 5 محاولات.
- إعادة إرسال الكود بعد 60 ثانية على الأقل، وبحد أقصى 5 أكواد للطلب في الساعة.
- جلسة التعديل بعد التحقق مدتها ساعة. لو انتهت، افتحي الطلب بكود جديد؛ لا تنشئي طلبًا جديدًا بدلًا منه.
- إتمام تحقق جديد لنفس الطلب يلغي صلاحية جلسة التحقق السابقة، لحماية البيانات.
- الأكواد وصلاحية الجلسة لا تُخزّن كنصوص صريحة؛ الأعمدة تخزّن بصماتها وتواريخ انتهائها. لا تعدّلي هذه الأعمدة يدويًا.
- الإيميل المسجّل ونوع التسجيل ثابتان خلال التعديل. تغييرهما يحتاج مراجعة المنظّم، حتى لا تتحول صلاحية فتح الطلب إلى شخص آخر.
- تعديل الطلبات ذات الحالة `Received` أو `Request received` أو `Updated` متاح. الطلبات الملغاة أو المرفوضة أو المؤكدة إداريًا ليست قابلة للتعديل الذاتي؛ يراجعها المنظّم.
- فتح الطلبات القديمة يعتمد على وجود Request ID بالصيغة الحالية و`Booking JSON` صالح. الطلب الأقدم الذي يفتقدها يحتاج مراجعة المنظّم، ولا نخمن تفاصيله.
- لو أي اختيار قديم أُزيل من كتالوج الفنادق أو النقل، يظل عرضه وPDF متاحين، لكن لا يُعدَّل تلقائيًا إلى بديل؛ يُطلب التواصل مع المنظّم.
- حافظي على الشيت ومجلد Drive بوضع **Restricted**، وشاركيهما فقط مع الموظفين الموثوقين. نشر Web App بوضع Anyone لا يجعل الشيت وDrive عامّين؛ طلبات الخدمة تظل تحتاج السر الخاص بالتطبيق.
- لا تضعي `BOOKING_API_TOKEN` في GitHub أو الواجهة، ولا تطلبي من العملاء إرساله.

## 4. الفاتورة والإيميل

- رقم الطلب ثابت، ورقم الفاتورة يميّز الإصدار:
  - الإصدار الأول: `INV-20260831-ABCDEF123456`
  - أول تعديل: `INV-20260831-ABCDEF123456-R2`
  - التعديل التالي: `INV-20260831-ABCDEF123456-R3`
- تُحفظ نسخة PDF جديدة للتعديل في نفس مجلد الفواتير، مع بقاء النسخ السابقة. تحتوي النسخة الجديدة رقم الإصدار وتاريخ التعديل واليورو فقط.
- تُرسل النسخة الجديدة إلى الإيميل المسجّل، وفي الرسالة رقم الطلب وطريقة فتحه للتعديل.
- تتم مطابقة بصمة PDF قبل إرساله وقبل توفير النسخة المحفوظة للتنزيل. إذا ظهر خلل سلامة، لا تعرضه الواجهة كملف موثوق.
- الطلب يُحفظ أولًا في Google؛ فشل PDF أو الإيميل لا يحذف الطلب ولا التعديل. تعرض الواجهة الحالة الفعلية بدل ادعاء إرسال لم يتم.
- من شاشة **View / edit existing request** يمكن تنزيل الفاتورة أو استعمال **Retry PDF / email**، حتى من متصفح جديد وبعد إغلاق الجلسة القديمة.
- إعادة محاولة المستندات تستخدم بيانات الطلب المحفوظة وأسعاره، لا حسابًا جديدًا بأسعار اليوم. أما تعديل بيانات الطلب فيستخدم الأسعار الحالية ويعرض الإجمالي قبل الحفظ.
- إذا لم يُنشأ PDF أصلًا، أو حُذف من Drive، استخدمي Retry PDF / email بعد فتح الطلب والتحقق. المحفز الدوري يعالج الإيميلات ذات PDF موجود فقط.
- لا يمكن ضمان إرسال الإيميل مرة واحدة تمامًا إذا انقطع التنفيذ بين الإرسال وتسجيل نجاحه. إعادة المحاولة لا تكرر الطلب أو حصة الغرف.
- PDF ملخص طلب، وليس إيصال دفع أو تأكيد حجز نهائي. قيود التعديل فيه تعتمد على احترام برنامج عرض PDF لها؛ ليست منعًا مطلقًا ولا توقيعًا رقميًا معتمدًا.
- يستخدم PDF نفس اللوجوهات المتاحة في `assets/logo1.png` و`logo2.png` و`logo3.png`. احتفظي بهذه الملفات عند التحديث.

## 5. الأوراق والأعمدة

| الورقة | وظيفتها |
|---|---|
| `Bookings` | صف واحد لكل طلب، يحتوي أحدث بياناته ورقم الإصدار وحالة آخر PDF وإيميل. |
| `Invoices` | صف لكل إصدار فاتورة؛ رقم Invoice No في Bookings يحدد النسخة الحالية. |
| `Request History` | نسخة من بيانات الطلب قبل كل تعديل، دون أكواد التحقق. سجل تاريخي، وليس استرجاعًا آليًا. |
| `Room Inventory` | حصة الغرف لكل فندق ونوع وليلة؛ لا تتغير قيمها من هذا التحديث. |

الأعمدة الجديدة في Bookings تشمل `Revision` و`Updated At` و`Last Edit ID` وأعمدة التحقق والجلسة ومعالجة المستندات. `setupSheetsNow` يضيفها تلقائيًا دون إعادة ترتيب الأعمدة الحالية.

لا تعدّلي تفاصيل الغرف أو الأسعار أو التواريخ في صف Bookings وحده؛ سيختلف عن `Booking JSON` وPDF. استخدمي مسار التعديل. يمكنك وضع Status = `Cancelled` أو `Rejected` لإيقاف الطلب وتحرير حصته. لا تحذفي الصف.

الحصص المبدئية السابقة (10 لكل نوع/ليلة عند الإعداد الأول) ليست أعدادًا مؤكدة من الفندق. راجعي الحصص الحقيقية قبل اعتماد الحجوزات النهائية. تغيير الأعداد إلى أعداد مؤكدة لا يتطلب إعادة كتابة التطبيق.

## 6. الإعدادات والحمل المتوقع

Streamlit Secrets — احتفظي بالقيم الموجودة:

```toml
GOOGLE_APPS_SCRIPT_URL = "رابط Web App الحالي المنتهي بـ /exec"
BOOKING_API_TOKEN = "السر الحالي نفسه الموجود في Google"
```

Apps Script → Project Settings → Script properties:

- `SPREADSHEET_ID`: نفس معرف الشيت الحالي.
- `BOOKING_API_TOKEN`: نفس السر في Streamlit.
- `INVOICE_FOLDER_ID`: نفس مجلد الفواتير الحالي. `DRIVE_FOLDER_ID` يظل بديلًا مدعومًا إن كان مستخدمًا سابقًا.
- `COMPANY_NAME`: اسم الجهة، اختياري.
- `PUBLIC_APP_URL`: رابط تطبيق Streamlit العام، اختياري لإضافته إلى إيميلات الفواتير. لا تضعي رابط الإدارة أو أي رمز سري.

الاستخدام المتوقع الآن حوالي **1000 طلب**، وكل طلب اتحاد قد يحتوي على أشخاص وغرف عديدة. لم نضع حدًا يقفل التسجيل عند 1000، ولم نقلّل الحد المسموح للأشخاص داخل طلب الاتحاد. عدد الطلبات الإجمالي مختلف عن عدد الطلبات المتزامنة وعن حصة الإيميلات اليومية. الفواتير وأكواد التحقق والإيميلات المعدلة كلها تستهلك حصة Google؛ لا يمكن ضمان وصول 1000 إيميل في نفس اليوم بالحساب الحالي دون مراجعة حصصه.

## 7. اختبار آمن قبل الإطلاق

استخدمي طلب اختبار بإيميل تملكينه، دون بيانات شخصية حقيقية غير ضرورية:

1. سجلي طلب اتحاد، وتحققي من وجود صف واحد في Bookings، وPDF في Drive، وصف Invoices، ووصول رسالة الفاتورة.
2. افتحي التطبيق في متصفح جديد → View / edit existing request. أدخلي الرقم والإيميل، وانتظري الكود، ثم افتحي الطلب.
3. عدّلي الغرف أو التواريخ أو خدمة نقل، وراجعي الإجمالي ثم Save Changes & Send Updated PDF.
4. تأكدي أن Request ID والصف الأصلي ثابتان، وRevision أصبح 2، وحُفظت النسخة القديمة في Request History، وظهرت فاتورة R2 في Drive وInvoices ووصلت بالإيميل.
5. افتحي نفس الطلب في جلستين، واحفظي من واحدة، ثم حاولي حفظ تعديل من الثانية: يجب أن يرفض النسخة القديمة ويطلب إعادة فتح الطلب.
6. افتحيه بإيميل غير مطابق أو كود خاطئ: يجب ألا تظهر أي بيانات.
7. افتحي طلب فرد وعدّلي دون تغيير الجواز: ينجح. جواز مستخدم في طلب آخر: يرفض. لا تنشئي جوازات اختبار تبدو حقيقية.
8. راجعي عرض Details والخانات والأزرار على الموبايل وPDF مع اللوجوهات الحقيقية. بعد الاختبار علّمي الطلب `Cancelled` بدل حذفه.

التحقق الذي نُفذ هنا محلي، باستخدام Streamlit AppTest ومحاكاة Google: حفظ الحالة والتنقل، التحقق الفوري، حساب الغرف والنقل، التحقق من الإيميل، التعديل على نفس الصف، تعارض الإصدارات، إعادة المحاولة، سجل الفواتير، ونماذج PDF تمت مراجعتها بصريًا. **لم تُرسل أي طلبات أو إيميلات حقيقية، ولم يُعدَّل مشروع Google المنشور تلقائيًا، ولم يُنفَّذ اختبار ضغط 1000 طلب متزامن.**

للمطور: بعد تثبيت requirements وpypdf، ومع وجود Node.js:

```bash
python -m unittest discover -s tests -p 'test_*.py' -v
```

ملفات tests للاختبار المحلي فقط؛ لا تنسخيها إلى Code.gs.
