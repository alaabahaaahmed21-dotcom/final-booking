# تحديث مشروع ITKF — تسجيل أفراد واتحادات

هذه ملفات تحديث للمشروع الحالي، وليس مطلوبًا إنشاء مشروع جديد أو Google Sheet جديد.
الإصدار: `2026-08-30-v2`.

## 1. قبل التحديث

- احتفظي بنسخة من كود المشروع ونسخة من Google Sheet قبل البدء.
- نفّذي التحديث في وقت لا يستقبل فيه التطبيق تسجيلات؛ التطبيق وكود Google يجب تحديثهما معًا.
- لا تمسحي الحجوزات القديمة، أو الصور القديمة، أو ملفات اللوجوهات، أو Streamlit Secrets.
- اختبري التحديث أولًا على نسخة تجريبية من الشيت ومجلد فواتير تجريبي إن أمكن.

## 2. تحديث Google Apps Script

1. افتحي **نفس مشروع Apps Script الحالي** المتصل بالتطبيق.
2. استبدلي محتوى ملف `Code.gs` بمحتوى `google_apps_script.gs` المرفق. لا تضيفي نسخة ثانية من نفس الدوال في ملف آخر. اعملي Save.
3. من **Project Settings → Script properties**، احتفظي بالقيم الحالية، وتأكدي من وجود:

| الخاصية | القيمة |
|---|---|
| `SPREADSHEET_ID` | `1CX6xn7zYmBO7086jQdfpouV8oHS5HsOR02oXsmtMYIU` |
| `INVOICE_FOLDER_ID` | `1RLmDW_mM4slH_r3tQtFqbo2Gw6ly4UEN` |
| `BOOKING_API_TOKEN` | نفس المفتاح السري الموجود في Streamlit Secrets، بدون نشره في GitHub أو الشيت |
| `COMPANY_NAME` | اختياري: اسم الجهة المرسلة للإيميل |

في الاختبار على نسخة تجريبية استخدمي معرّفات النسخة التجريبية بدلًا من المعرفات أعلاه.
الإصدار يدعم `DRIVE_FOLDER_ID` كبديل لمجلد الفواتير إذا لم توجد `INVOICE_FOLDER_ID`.
لا يحتاج إلى مجلد رفع صور جديد.

4. من قائمة الدوال بجوار **Run / Debug** اختاري `setupSheetsNow` ثم **Run** مرة واحدة. وافقي على الصلاحيات إذا طلب Google ذلك بعد مراجعتها.
5. ستُجهّز تبويبات `Bookings` و`Invoices` و`Room Inventory`. الأعمدة الناقصة تُضاف دون حذف الأعمدة أو البيانات القديمة. الأعمدة القديمة للعملات والصور تبقى تاريخية؛ الطلبات الجديدة لا تكتب بها.
6. شغّلي `diagnoseBackend` للتأكد من وصول الحساب إلى الشيت ومجلد الفواتير ومعرفة رصيد إرسال الإيميلات في Execution log.
7. شغّلي `installRetryTrigger` مرة واحدة إذا أردت إعادة محاولة إرسال الإيميلات المعلقة تلقائيًا كل 5 دقائق. هذا يثبت Trigger داخل حساب Google الخاص بك.
8. **Deploy → Manage deployments → علامة القلم → Version: New version → Deploy**. احتفظي بنفس رابط `/exec`.
9. إعدادات الـWeb App: **Execute as: Me** و **Who has access: Anyone**. هذا يسمح للتطبيق باستدعاء الخدمة؛ كل طلب كتابة ما زال يحتاج المفتاح السري. **الشيت ومجلد الفواتير يظلان Restricted**، ولا تضعي المفتاح في واجهة العميل.

## 3. تحديث ملفات التطبيق في نفس GitHub repository

فكي ضغط الملف. الملفات داخله موجودة مباشرة، دون مجلد مشروع إضافي.
ارفعي/استبدلي هذه الملفات في جذر مشروع `final-booking`، بجوار `app.py` الحالي، في Commit واحد:

- `app.py`
- `config.py`
- `helpers.py`
- `countries.py`
- `sheets.py`
- `pdf_generator.py`
- `requirements.txt`

احتفظي باسم **`helpers.py`** بالضبط؛ النسخة الجديدة تستخدم `from helpers import ...` ولا تستخدم `booking_helpers.py`.
لا تخلطي `app.py` الجديد مع ملفات مساعدة من نسخة قديمة.
ملف `uploads.py` القديم لم يعد مستخدمًا؛ لا يلزم حذفه حتى يعمل التحديث.

أضيفي أيضًا:

- `.streamlit/config.toml` لضبط Light Mode. **لا تستبدلي ملف secrets.toml ولا تضعي الأسرار في GitHub**. إذا كان config.toml الحالي يحتوي إعدادات إضافية مهمة، ادمجي إعدادات `[theme]` الجديدة معه بدل حذفها.
- مجلد `assets/fonts` المرفق؛ الخطوط تساعد في عرض الأسماء العربية داخل PDF.

احتفظي باللوجوهات الأصلية في `assets/logo1.png` و`assets/logo2.png` و`assets/logo3.png`. سيقرأها التطبيق وPDF تلقائيًا. اللوجوهات نفسها ليست ضمن ملف التحديث؛ **ادمجي مجلد assets ولا تمسحي محتواه القديم**.

في Streamlit Secrets احتفظي بنفس:

```toml
GOOGLE_APPS_SCRIPT_URL = "رابط الويب آب الحالي المنتهي بـ /exec"
BOOKING_API_TOKEN = "نفس المفتاح في Script properties"
```

الاسم القديم `GOOGLE_SHEET_API` مدعوم أيضًا إذا لم يوجد `GOOGLE_APPS_SCRIPT_URL`.
انتظري إعادة تثبيت المكتبات وإعادة تشغيل التطبيق، أو اختاري Reboot app.
لو ظهر تنبيه اختلاف الإصدار، حدّثي **Deployment** في Google؛ مجرد Save لا يحدث النسخة المنشورة.

## 4. إدارة الغرف المتاحة

تبويب `Room Inventory` خاص بالإدارة. يُنشئ الإعداد الأول **10 غرف لكل نوع غرفة في كل فندق** كحصة داخلية مبدئية، قابلة للتعديل. لا يظهر للعميل تنبيه بأن الأعداد مبدئية.

| Hotel | Room Type | Date | Capacity |
|---|---|---|---|
| Tiba Rose El Golf | Double | * | 10 |
| Tiba Rose El Golf | Double | 2026-10-25 | 6 |

- `Capacity` هو **إجمالي الحصة** لهذا النوع، وليس العدد المتبقي. لا تخصمي الطلبات يدويًا من هذا الرقم.
- `*` تعني الحصة الافتراضية لكل الليالي. صف بتاريخ محدد يستبدل الحصة الافتراضية لتلك الليلة فقط؛ المثال يجعل حصة الدبل 6 يوم 25 أكتوبر.
- التاريخ يُكتب `YYYY-MM-DD`. لا تكرري نفس الفندق/نوع الغرفة/التاريخ في صفين.
- لا تغيّري تهجئة أسماء الفنادق أو الغرف. عدّلي Capacity فقط عند وصول الأعداد النهائية.
- حصة الصفر تمنع الطلبات الجديدة لذلك النوع في تلك الليلة. نفاد كل الأنواع يمنع الطلب في الفندق لهذه التواريخ، ولا يغلقه في تواريخ أخرى متاحة.
- الحجز يحسب ليلة الوصول ويستبعد ليلة المغادرة. وجبات مختلفة لا تنشئ مخزون غرف منفصلًا.
- فحص الحصة النهائي يجري في Google تحت قفل مشترك لحظة الحفظ؛ فحص الواجهة وحده لا يحجز غرفة.
- بمجرد تسجيل الطلب بنجاح في `Bookings` يُحسب ضمن الحصة. إعادة إرسال نفس Request ID لا تحجز غرفًا إضافية.
- لو أردت إلغاء طلب، عدّلي `Status` إلى `Cancelled`؛ وإذا رفضتِه إلى `Rejected`. لا يُحسب بعدها ضمن الحصة. لا تحذفي الصف لكي يظل سجل الطلب محفوظًا.
- الطلبات القديمة الصالحة تدخل في الحساب؛ إن كانت قديمة بلا حقل عدد غرف، تُحسب غرفة واحدة كما كان النظام القديم. إذا كانت تواريخ أو أنواع غرف قديمة غير مفهومة، يتوقف قبول الفندق حتى مراجعة تلك الصفوف؛ لا يتجاهلها الكود ويبيع فوق المتاح.
- إعادة تشغيل `setupSheetsNow` لا تعيد القيم المعدلة إلى 10.

الأعداد المبدئية ليست تأكيدًا من الفندق. رسالة العميل هي استلام الطلب ورقمه، وليست ادعاء دفع المبلغ أو تأكيد الفندق. راجعي الحصص النهائية مع الفنادق قبل اعتماد التأكيدات النهائية.

## 5. التسجيل والمواصلات

**Individual:** الاسم بحروف كبيرة، الجنسية من قائمة، تاريخ الميلاد، رقم الجواز، الإيميل، ورقم الهاتف بكود الدولة في خانة واحدة. لا يوجد رفع صور. رقم الجواز يُوحّد إلى حروف كبيرة دون مسافات/شرطات، ويُمنع تكراره في Google حتى مع طلبين متزامنين.

**Federation:** اسم الاتحاد، الإيميل، الهاتف الدولي فقط للبيانات الشخصية؛ يمكن اختيار عدة غرف من أنواع مختلفة داخل الفندق المختار. عدد الضيوف يساوي مجموع الغرف × سعة كل نوع. الأفراد يحجزون غرفة واحدة، والاتحاد يستطيع حجز أكثر من غرفة.

**المواصلات:** كل الأسعار التالية للعربية بالكامل وباليورو، شاملة 14% VAT، وليست للفرد. لا يُضاف VAT مرة ثانية.

| Vehicle | Airport / Hotel one-way | Hotel / Stadium one-way | Daily 8h | Daily 12h |
|---|---:|---:|---:|---:|
| Limousine (3 Seats) | 30 | 30 | 110 | 145 |
| H1 / Van (7 Seats) | 50 | 50 | 150 | 170 |
| Toyota Hiace (10 Seats) | 60 | 60 | 160 | 200 |
| Coaster (26 Seats) | 100 | 100 | 200 | 240 |
| Bus (50 Seats) | 190 | 190 | 300 | 400 |

- بند توصيل الفرد €10 وأسعار المواصلات القديمة أُزيلت.
- اختاري نوع الخدمة والاتجاه ثم اليوم ومن الساعة إلى الساعة، وكل الأوقات بتوقيت القاهرة المحلي.
- لكل يوم أو رحلة إضافية استخدمي **Add another day / service**. الذهاب والعودة خدمتان منفصلتان.
- إذا انتهت الخدمة بعد منتصف الليل، فعّلي **End time is on the next day**. اليوم الكامل لا يتجاوز 8 أو 12 ساعة حسب الباقة المختارة، والسعر سعر الباقة وليس سعر الساعة.
- يمكن اختيار أكثر من نوع عربية وبأكثر من عدد في نفس الخدمة.
- مثال 60 شخصًا وباص 50: يظهر أن 10 أشخاص يحتاجون مقاعد إضافية، وتقترح الواجهة العربيات المناسبة. العميل يختار بنفسه. باص 50 + Hiace 10 = **€250** للتوصيلة الواحدة.
- لا يُقبل الطلب النهائي إذا كان عدد المقاعد أقل من الركاب.
- لا يوجد مخزون عدد عربيات، لأن أعداد أسطول النقل لم تُزوَّد. فحص النقل الحالي للسعة المطلوبة وعدد المقاعد، وليس تأكيد توافر فعلي من شركة النقل.
- أسعار الفنادق وطريقة حسابها القديمة لم تتغير: السعر الحالي في الكود × عدد الغرف × الليالي، دون ضرب إضافي في عدد الضيوف. راجعي أساس تسعير الفنادق مع الجهة قبل فتح التسجيل العام، خصوصًا لو كانت أي أسعار متفق عليها للفرد وليست للغرفة.

## 6. التسجيل والفاتورة والإيميل

- `Bookings`: السجل الأساسي لجميع الطلبات، ويُحفظ أولًا. توجد به بيانات الغرف والخدمات كاملة في حقول JSON بالإضافة للأعمدة الأساسية.
- `Invoices`: سجل نسخة PDF ورقمها وبصمتها وحالة إرسالها.
- PDF باليورو فقط، يتضمن اللوجوهات المتاحة ورقمًا فريدًا مرتبطًا بالطلب، ويُحفظ في مجلد الفواتير ويُرسل كمرفق للعميل.
- PDF ملخص طلب وليست إيصال سداد. لا يذكر أن العميل دفع.
- قيود تعديل PDF تمنع التعديل في البرامج التي تحترمها، لكنها ليست منعًا مطلقًا ولا توقيعًا رقميًا معتمدًا. نسخة Drive الأصلية وبصمة SHA-256 تساعدان على مقارنة أي نسخة معدلة.
- إذا فشل إنشاء PDF أو حفظه أو إرسال الإيميل، لا يُحذف الطلب المحفوظ. تظهر الحالة Pending؛ يمكن استخدام **Retry PDF / email** من شاشة الإتمام بنفس Request ID.
- المهمة الدورية تعيد إرسال PDF الموجود بالفعل في Drive. لو لم يُنشأ أصلًا، يجب استخدام Retry PDF من التطبيق. لو أغلقتِ الجلسة قبل ذلك، يلزم دعم الإدارة باستخدام بيانات الطلب؛ لا تسجلي نفس الحجز كطلب جديد.
- إذا انقطع الرد أثناء الحفظ، استخدمي **Retry saving** ولا تبدئي طلبًا جديدًا. في حالة عدم التأكد، راجعي Request ID في الشيت أولًا.
- لا يمكن ضمان إرسال الإيميل مرة واحدة تمامًا إذا انقطع التنفيذ في اللحظة بين إرساله وتسجيل نجاحه؛ إعادة المحاولة لا تكرر الحجز أو خصم الغرف.
- الحساب الشخصي له حدود يومية للإرسال والتنفيذ من Google. النظام يحتفظ بالطلب عند بلوغ حصة الإيميل، لكن هذا لا يضمن 5000 إيميل في يوم واحد أو 5000 تسجيل متزامن. راجعي الحصص وخطة الاستضافة قبل حملة تسجيل كبيرة.

## 7. اختبار قبل فتحه للناس

1. سجلي طلبًا تجريبيًا فرديًا بإيميل تستطيعين فتحه، ثم طلب اتحاد بغرفتين وخدمتي نقل في يومين.
2. تأكدي من ظهور كل Request ID مرة واحدة في `Bookings`، وتفاصيل الغرف/النقل، ورقم PDF في `Invoices`، ونسخته في Drive، ووصول الإيميل.
3. جرّبي نفس جواز السفر مرة ثانية: يجب رفضه دون صف جديد.
4. على النسخة التجريبية، ضعي الحصة 1 لنوع غرفة في ليلة محددة، ثم أرسلي طلبين متداخلين: الأول يُقبل، والثاني يُرفض. لا تفعلي هذا على الحجوزات الفعلية.
5. راجعي Next وBack، الحقول على الموبايل، وPDF مع اللوجوهات. بعد الاختبار أعيدي أعداد الحصص المطلوبة وعلّمي طلبات الاختبار `Cancelled` بدل حذفها.

التحقق المنفذ مع هذه النسخة محلي: 21 اختبارًا تشمل التنقل وحفظ الحقول والأسعار ومنع الجواز المكرر والمحاكاة المتزامنة وإعادة المحاولة وحصص الغرف وPDF. **لم تُرسل أي تسجيلات أو إيميلات حقيقية ولم يُحدَّث مشروع Google المنشور تلقائيًا.**

للمطور: من جذر المشروع بعد تثبيت requirements وpypdf، ومع وجود Node.js:

```bash
python -m unittest discover -s tests -p 'test_*.py' -v
```

ملفات tests للاختبار فقط، لا تحتاجين تشغيلها في Apps Script أو تغيير اسم التطبيق بسببها.
